# TODO: Profile Page Implementation

## Steps

- [x] 1. `database/db.py` — Add `get_user_by_id(user_id)` helper
- [x] 2. `database/db.py` — Add `get_user_expenses_summary(user_id)` helper
- [x] 3. `app.py` — Update imports to include new helpers
- [x] 4. `app.py` — Rewrite `/profile` route with auth guard, DB queries, template render
- [x] 5. `templates/profile.html` — Create profile page template (info card, stats, category bars, recent table)
- [x] 6. `static/css/profile.css` — Create profile page styles with CSS variables
- [x] 7. `templates/base.html` — Add "Profile" nav link for logged-in users
- [x] 8. Run `python app.py` — Server started successfully on port 5001, no errors

