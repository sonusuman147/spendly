# TODO: Fix Registration and Login Implementation

## Steps

- [x] 1. Create TODO.md — track progress
- [x] 2. `database/db.py` — Add `create_user(name, email, password)` helper
- [x] 3. `app.py` — Fix `/register` route to accept GET and POST
- [x] 4. `templates/register.html` — Fix form action, add confirm_password, use flash messages
- [x] 5. Run `python app.py` and verify all functionality — Server started successfully on port 5001, no errors

